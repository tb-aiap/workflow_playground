[project]
name = "workflow-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest-cov>=7.0.0",
    "ruff>=0.13.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
